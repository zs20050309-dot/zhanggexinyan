# 产品开发规划 · 宏观方向文档
## 抖音内容识别器
版本 v1.0 | 开发启动前阅读

---

## 一、整体模块地图

产品由 **6个核心模块** 组成，外加 **2个支撑模块**。

```
                    ┌─────────────────┐
                    │   前端应用 (F)   │
                    │  Web界面 + 交互  │
                    └────────┬────────┘
                             │ HTTP请求
                    ┌────────▼────────┐
                    │  后端API服务 (G) │
                    │  调度 + 状态管理  │
                    └──┬──┬──┬──┬──┬─┘
                       │  │  │  │  │
          ┌────────────┘  │  │  │  └─────────────┐
          │               │  │  │                 │
    ┌─────▼─────┐  ┌──────▼──┐ │ ┌──────────┐ ┌──▼──────────┐
    │ 视频获取   │  │  AI诊断  │ │ │ 动态问卷 │ │  报告生成   │
    │  模块 (A)  │  │ 引擎 (C) │ │ │ 系统 (D) │ │  模块 (E)  │
    └─────┬─────┘  └──────┬──┘ │ └──────────┘ └─────────────┘
          │               │    │
    ┌─────▼─────┐  ┌──────▼──┐ │
    │  ASR转录   │  │ 实时搜索 │ │
    │  模块 (B)  │  │ 模块 (C')│─┘
    └───────────┘  └─────────┘

(F) 前端      → 用户界面与交互
(G) 后端      → 请求调度、状态管理、链接存储
(A) 视频获取  → 链接解析、音频提取、元数据获取
(B) ASR转录   → 音频转文字
(C) AI诊断    → 内容类型识别、风险评分
(C') 实时搜索 → 信息差收割型专用，核实时效性
(D) 动态问卷  → AI实时生成问题、对话状态管理
(E) 报告生成  → 个性化报告、识别公式输出
```

---

## 二、模块详细规划

---

### 模块 A：视频内容获取

**这个模块做什么**

接收用户输入的抖音视频链接，解析出三类原始信息：
- **视频元数据**：标题、发布账号名、发布时间、点赞/评论/转发量
- **音频文件**：供ASR模块转录
- **降级文本**：如果音频无法获取，尝试获取字幕或视频描述文字

**技术方向**

优先尝试官方路径：
- 抖音开放平台 API（需申请权限，稳定但受限）

官方路径不通时的备选：
- `yt-dlp`（开源，支持抖音，稳定性较好）
- 第三方抖音解析API（速度快但有成本和稳定性风险）

最终降级方案（保证Demo不崩溃）：
- 用户手动粘贴视频文字内容或标题，跳过自动解析

**潜在问题**

| 问题 | 严重程度 | 说明 |
|---|---|---|
| 平台反爬限制 | ⚠️ 高 | 抖音对自动化访问有检测，可能频繁失效 |
| 私密视频无法解析 | 中 | 仅限公开视频，需在UI上明确告知 |
| 音频解析延迟 | 中 | 视频较长时下载+解析可能超过10秒 |
| 链接格式多样 | 低 | 抖音分享链接格式不统一，需做兼容处理 |

**开发建议**

这个模块是整个产品最不稳定的环节，建议第一天先用`yt-dlp`跑通流程，同时把"手动粘贴文字"的降级方案做好，不要让这个模块卡住整体进度。

---

### 模块 B：ASR语音转文字

**这个模块做什么**

将模块A获取的音频文件转换为文字稿，供后续所有AI分析使用。这是AI能"理解"视频内容的基础。

**技术方向**

主选方案：
- **OpenAI Whisper API**：准确率高，支持中文，按用量收费，调用简单
- **字节火山引擎 ASR**：与抖音同属字节，中文识别效果好，且在比赛场景下可能有API额度支持

备选方案：
- 本地部署 Whisper 开源模型（免费但速度慢，对算力有要求）
- 阿里云/腾讯云 ASR（稳定，按量付费）

**潜在问题**

| 问题 | 严重程度 | 说明 |
|---|---|---|
| 转录速度影响体验 | ⚠️ 高 | 1分钟音频约需3-8秒，3分钟视频可能需要15秒以上 |
| 背景音乐干扰识别 | 中 | 抖音视频BGM较重时准确率下降 |
| 方言识别 | 低 | 普通话为主，影响有限 |
| API调用成本 | 低 | 黑客松期间量不大，可控 |

**开发建议**

转录速度是用户体验的关键瓶颈。建议：
1. 转录过程前端实时显示进度（"正在解析视频内容..."）
2. 三个预置Demo案例的转录结果提前缓存，现场展示不经过这个模块

---

### 模块 C：AI诊断引擎

**这个模块做什么**

接收结构化的视频信息（文字稿 + 元数据），输出诊断结果：
- 内容类型标签（四类，支持复合）
- 操控风险评分（0-100）
- 核心问题一句话
- 是否需要触发实时搜索

**技术方向**

核心是 **Prompt Engineering**，不涉及复杂的工程实现，但Prompt质量直接决定产品核心价值。

模型选择：
- **Claude claude-sonnet-4-20250514**（推理能力强，对论点分析任务效果好）
- **GPT-4o**（备选，综合能力强）
- **DeepSeek V3**（国产，成本低，中文理解好，可作为成本敏感的替代）

输出格式：要求模型严格输出 JSON，方便前端解析。

**潜在问题**

| 问题 | 严重程度 | 说明 |
|---|---|---|
| JSON输出不稳定 | ⚠️ 高 | 大模型有时不严格遵守格式，需要解析容错处理 |
| 误判正常内容 | ⚠️ 高 | 把正常内容判成操控性内容，损害产品可信度 |
| 类型判断不准确 | 中 | 四类边界模糊时容易判错，需大量测试 |
| 评分一致性差 | 中 | 相似内容给出差异较大的评分，影响可信度 |

**开发建议**

这个模块需要花最多时间调 Prompt。建议：
1. 准备10-15条测试用例（正面/负面各类型），每次改Prompt都跑全量测试
2. 在系统Prompt里加入大量反例（"以下情况不应该被识别为操控性内容"）
3. 评分部分用区间锚定（给模型几个参考案例和对应分数）而不是让模型自由打分

---

### 模块 C'：实时搜索增强

**这个模块做什么**

专门为"信息差收割型"内容服务。在AI诊断判定需要搜索后，调用实时搜索API，核实：
- 视频中提到的趋势/事件当前是否仍然成立（时效性）
- 影响范围是否如视频所述（范围核实）

搜索结果以2-3句话的形式注入报告生成的上下文。

**技术方向**

主选方案：
- **Perplexity API**：搜索质量高，返回已整合的自然语言结果，适合直接注入Prompt
- **Tavily API**：专为AI应用设计的搜索API，返回结构化结果

备选方案：
- **Serper API**（谷歌搜索结果）+ 大模型二次整合
- 直接在大模型调用中开启`web_search`工具（部分模型支持）

**潜在问题**

| 问题 | 严重程度 | 说明 |
|---|---|---|
| API稳定性 | ⚠️ 高 | 黑客松期间网络环境可能不稳定 |
| 搜索结果质量 | 中 | 有时返回不相关内容，需要做质量过滤 |
| 额外延迟 | 中 | 搜索调用增加2-5秒，需在UI上告知用户 |
| 中文搜索效果 | 中 | 部分API对中文内容支持有限 |

**开发建议**

这个模块不是MVP的必须项。建议：
1. 先把核心链路打通，最后才接入搜索
2. 做好降级处理：搜索失败时，报告中这部分用"基于现有信息"代替，不影响其他部分输出
3. 预置Demo的搜索结果可以缓存，避免现场网络问题

---

### 模块 D：动态问卷系统

**这个模块做什么**

这是产品最核心的差异化体验。通过多轮AI对话，动态获取用户背景信息，为个性化报告提供输入。

每一轮的逻辑：
```
[视频诊断结果] + [历史对话] + [用户当前回答]
→ AI决定：继续提问 OR 结束对话
→ 若继续：输出下一个问题（类型 + 选项）
→ 若结束：触发报告生成
```

**技术方向**

**前端状态管理：**
- 维护对话历史数组（每轮问题+答案）
- 管理当前问题展示状态（逐个出现的动效）
- 处理选择题/填写题的不同渲染

**后端对话状态：**
- 每次请求传入完整的对话历史（无状态API设计，不依赖服务端session）
- 或使用Redis维护session（更复杂但更稳定）
- 黑客松推荐无状态设计：前端维护对话历史，每次请求带全量传给后端

**AI调用：**
- 每轮对话触发一次AI调用（3-5轮共3-5次调用）
- 要求模型输出严格JSON：`{action: "ask"/"finish", question: "...", type: "choice"/"text", options: [...]}`

**潜在问题**

| 问题 | 严重程度 | 说明 |
|---|---|---|
| 问题质量不稳定 | ⚠️ 高 | 模型有时生成无关或重复的问题 |
| 停止条件判断不准 | ⚠️ 高 | 模型可能在信息不足时就结束，或问超过5个问题 |
| 多次AI调用成本/延迟 | 中 | 3-5次调用叠加，总等待时间可感知 |
| 前端状态同步复杂 | 中 | 对话历史的维护和传递需要仔细设计 |
| JSON格式失败 | 中 | 解析失败时需要有fallback处理 |

**开发建议**

1. Prompt里要给模型非常明确的停止条件（"当你已知用户的X、Y、Z信息时，输出finish"）
2. 前端做好JSON解析容错，任何格式错误都有备用展示方案
3. 每次AI调用加上超时处理（5秒超时，显示"正在思考..."不让用户干等）

---

### 模块 E：报告生成

**这个模块做什么**

整合所有已有信息，生成最终的个性化分析报告：

输入：
- 视频文字稿 + 元数据
- AI诊断结果
- 完整用户背景（对话历史）
- 实时搜索结果（如有）

输出：
- 按标准结构的Markdown格式报告
- 包含：视频解析 + 个性化分析 + 事实核查（条件触发）+ 识别公式

**技术方向**

**AI调用：**
- 单次调用，但上下文较长（需计算token消耗）
- 要求流式输出（streaming）：报告内容边生成边显示，避免用户等待

**前端渲染：**
- 流式接收Markdown文本，实时渲染（可用`react-markdown`库）
- 分章节的渲染效果（每个章节出现时有轻微动效）

**报告存储（分享功能）：**
- 报告生成后存储到KV数据库（Vercel KV / Redis / 简单的JSON文件）
- 生成短链接（UUID），用于分享
- 存储内容不包含用户个人背景信息（隐私保护）

**潜在问题**

| 问题 | 严重程度 | 说明 |
|---|---|---|
| 个性化程度不足 | ⚠️ 高 | 报告看起来像模板而不是针对用户，是最常见的失败 |
| 报告过长 | 中 | 模型容易输出过多内容，需要在Prompt里约束长度 |
| Markdown格式混乱 | 中 | 模型有时输出不规范的Markdown，影响渲染 |
| Token超限 | 中 | 长视频文字稿 + 完整对话历史可能接近上下文限制 |
| 流式渲染闪烁 | 低 | 技术问题，需要前端处理好chunk的渲染逻辑 |

**开发建议**

1. Prompt里明确指定报告的每个章节，且每章都要求引用用户的具体背景信息（防止输出通用内容）
2. 对视频文字稿做长度截断处理（超过2000字只取前后各1000字），防止token超限
3. 先不做流式渲染，loading完再整体渲染；流式渲染作为体验优化项

---

### 模块 F：前端应用

**这个模块做什么**

用户与产品交互的唯一界面。需要支持：
- 链接输入和提交
- 诊断结果的视觉化展示（类型标签、风险仪表盘）
- 动态问卷的逐步交互（选择题 + 填写题）
- 报告的渲染展示（Markdown）
- 分享链接的生成和访问

**技术方向**

框架选型：
- **Next.js**（推荐）：前后端一体，部署到Vercel开箱即用，支持API Routes
- 或 **React + FastAPI**：前后端分离，灵活但部署稍复杂

样式：
- **Tailwind CSS**：快速开发，黑客松首选

关键依赖库：
- `react-markdown`：Markdown渲染
- `framer-motion`：轻量动效（问题逐个出现、结果加载动画）
- Axios / fetch：API调用

**潜在问题**

| 问题 | 严重程度 | 说明 |
|---|---|---|
| 移动端适配 | 中 | 评审可能用手机体验，需要响应式设计 |
| 流式渲染实现 | 中 | SSE或fetch streaming的前端处理有一定复杂度 |
| 页面状态管理 | 中 | 从输入→诊断→问卷→报告的多步骤状态需要清晰设计 |
| 加载体验 | 低 | 多次AI调用的等待期需要用进度/动画填充 |

**开发建议**

1. 页面做5个：首页、诊断结果页、问卷页、报告页、分享报告页
2. 状态管理不要过度设计，用React Context或简单的useState链式传递足够
3. 移动端最低要求：在手机上可以完整体验，不会出现布局错乱

---

### 模块 G：后端API服务

**这个模块做什么**

所有AI调用和第三方服务的调度层。负责：
- 接收前端请求，编排各模块的调用顺序
- 管理对话状态（或传递前端维护的状态）
- 处理错误和超时，返回友好的降级结果
- 报告存储和短链接生成

**技术方向**

框架选型：
- **Python FastAPI**（推荐）：异步支持好，AI库生态丰富，团队可能更熟悉
- 或 **Next.js API Routes**：如果前端用Next.js，可以不单独部署后端

部署方案：
- Vercel（前端 + API Routes，零运维）
- Railway / Render（FastAPI，简单部署）
- 本地运行（黑客松现场可以接受，但有风险）

**潜在问题**

| 问题 | 严重程度 | 说明 |
|---|---|---|
| AI调用超时 | ⚠️ 高 | 大模型调用有时超过30秒，需要设置合理超时和重试 |
| 并发请求冲突 | 低 | 黑客松期间用户量小，基本不是问题 |
| API Key管理 | 中 | 不能硬编码在代码里，用环境变量管理 |
| 部署不稳定 | 中 | 黑客松现场网络环境复杂，最好提前部署到云上 |

---

## 三、模块依赖关系

开发时需要按依赖顺序推进，不能并行开发相互依赖的模块：

```
独立开发（可并行）：
├── 模块A（视频获取）
├── 模块B（ASR转录）
└── 模块F前端骨架（页面结构，暂时用mock数据）

第一层依赖（A+B完成后）：
└── 模块C（AI诊断）← 需要A+B输出的文字稿

第二层依赖（C完成后）：
├── 模块C'（实时搜索）← 需要C的诊断结果触发
└── 模块D（动态问卷）← 需要C的诊断结果作为上下文

第三层依赖（D+C'完成后）：
└── 模块E（报告生成）← 需要D的对话结果 + C'的搜索结果

最后集成：
└── 模块G（后端）← 把所有模块串起来
└── 模块F（前端）← 接入真实API替换mock数据
```

---

## 四、技术栈总览

| 层级 | 技术选型 | 备注 |
|---|---|---|
| 前端框架 | Next.js + React | 前后端一体化部署 |
| 样式 | Tailwind CSS | 快速开发 |
| 后端框架 | FastAPI（Python）或 Next.js API Routes | 按团队熟悉度选 |
| 核心AI模型 | Claude claude-sonnet-4-20250514 / GPT-4o | 推理能力优先 |
| 备用AI模型 | DeepSeek V3 | 成本控制 |
| 视频解析 | yt-dlp | 开源，最稳定的备选方案 |
| ASR转录 | OpenAI Whisper API 或 火山引擎ASR | 中文优先选火山 |
| 实时搜索 | Perplexity API 或 Tavily | Perplexity质量更好 |
| 部署 | Vercel（前端）+ Railway（后端）| 快速部署 |
| 数据存储 | Vercel KV 或 Redis | 仅存分享报告 |
| 版本管理 | GitHub | 提交材料要求 |

---

## 五、关键决策点

开发启动前，团队需要明确以下几个问题，后续所有细节都依赖这些决策：

**决策1：前后端架构**

选项A：Next.js全栈（前端 + API Routes）
- 优点：一套代码，Vercel一键部署，黑客松首选
- 缺点：AI调用较重的逻辑放在API Routes里有超时风险（Vercel默认10秒限制）

选项B：React前端 + FastAPI后端
- 优点：后端更灵活，AI调用无超时限制
- 缺点：需要分别部署，增加运维复杂度

**建议：** 如果团队有Python后端经验，选B；如果团队以前端为主，选A并配置Vercel的超时设置。

---

**决策2：视频获取的底线方案**

模块A是最不可控的。团队需要在第一天就确认：
- yt-dlp能否在当前环境正常解析抖音链接？
- 如果不行，手动粘贴文字的降级方案是否在Demo中可接受？

**建议：** 第一个小时就跑通链接解析，不行立刻切降级方案，不要在这里消耗时间。

---

**决策3：问卷的AI调用策略**

选项A：每轮问卷对话都调用AI决定下一个问题（完全动态，质量高）
- 缺点：3-5次额外AI调用，每次1-3秒，累计延迟明显

选项B：第一次诊断时AI一次性生成所有问题（伪动态，实现简单）
- 缺点：失去真正动态追问的能力

**建议：** 先实现选项B快速打通链路，在有时间的情况下升级为选项A。

---

**决策4：AI模型选择**

不同模型的权衡：

| 模型 | 推理质量 | 速度 | 成本 | 中文效果 |
|---|---|---|---|---|
| Claude claude-sonnet-4-20250514 | ⭐⭐⭐⭐⭐ | 中 | 中 | 好 |
| GPT-4o | ⭐⭐⭐⭐ | 快 | 中 | 好 |
| DeepSeek V3 | ⭐⭐⭐ | 快 | 低 | 很好 |

**建议：** 诊断和报告生成用Claude或GPT-4o保证质量，动态问卷用DeepSeek V3降低成本（问卷质量要求相对低）。

---

## 六、MVP范围定义

黑客松25小时内，哪些是必须做的，哪些是加分项：

### 必须完成（MVP）

- [ ] 视频链接输入（或手动粘贴文字降级方案）
- [ ] 内容类型识别 + 风险评分 + 核心问题（诊断层）
- [ ] 至少3轮的用户背景问卷（可以是伪动态）
- [ ] 个性化分析报告输出（含识别公式）
- [ ] 三个预置Demo案例可完整跑通
- [ ] 基础UI可用，PC端可正常体验

### 完成后做（加分项，按优先级排序）

1. **真正的动态问卷**（每轮AI实时决定问题）
2. **流式输出**（报告生成边生成边显示）
3. **实时搜索增强**（信息差收割型的事实核查）
4. **报告分享功能**（生成可转发链接）
5. **移动端适配**（手机上可正常使用）
6. **内容推荐**（用户触发的相关内容推荐）

---

## 七、最大风险项汇总

按风险程度排序，开发前需要有预案：

**风险1：视频链接无法自动解析（高风险）**
- 预案：第一天确认，不行立刻切手动输入降级方案，不要死磕

**风险2：AI诊断的误判损害可信度（高风险）**
- 预案：大量测试，在Prompt里加入足够的反例；UI上加"仅供参考"提示

**风险3：动态问卷输出不稳定（高风险）**
- 预案：先用伪动态方案保底；真动态方案作为加分项

**风险4：全链路延迟过高（中风险）**
- 视频获取（3-5s）+ ASR（5-15s）+ 诊断（2-5s）+ 问卷×N（N×2s）+ 报告（5-10s）
- 总延迟可能超过40秒
- 预案：预置Demo案例缓存；UI全程显示进度，告知用户当前在做什么

**风险5：现场网络环境问题（中风险）**
- 预案：提前部署到云上；Demo用缓存结果；准备本地备份版本

---

## 八、下一步

这份文档是宏观方向层面的规划，确认后需要进一步输出：

1. **详细技术文档**：每个模块的具体实现方案、接口定义、数据结构
2. **Prompt工程文档**：三个核心Prompt的完整版本和调试记录
3. **前端交互设计**：页面原型和组件拆分
4. **测试用例库**：覆盖四类内容的测试案例集，用于Prompt调试

建议开发启动前，团队对这份文档做一次对齐会议，明确以下事项后开始动手：
- 前后端架构选型（决策1）
- 每人负责哪个模块
- 第一个小时先跑视频解析（验证决策2）

---

*文档版本 v1.0 | 宏观方向规划，不含代码细节*
*下一层文档：各模块详细技术文档（开发启动后输出）*
